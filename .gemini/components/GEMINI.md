# Components Directory Context

You are working in the components directory.

**Load**: `@.agents/skills/nextjs-frontend.md` for component patterns.

**Remember**:
- Follow @AGENTS.md protocol
- One component per file
- Use shadcn/ui from `components/ui/`
- Follow component-structure rules
- Load all rules from `.agents/rules/`

**Directory Organization**:
- `ui/` - shadcn/ui components (managed by shadcn)
- `layout/` - Layout components (Header, Footer, etc.)
- `features/` - Feature-specific components
- `shared/` - Shared utility components
